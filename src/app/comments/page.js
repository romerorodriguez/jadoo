import styles from "./comments.module.css"

export default function Comments() {
    return (
        <div className={styles.container}>
            <h2>Hola, esta es la pantalla de comentarios</h2>
        </div>
    )
}